meta {
  name: Get All Brands
  type: http
  seq: 2
}

get {
  url: {{host}}/brands
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.statusCode: eq 200
  res.body.isSuccess: isBoolean
  res.body.isSuccess: eq true
}

settings {
  encodeUrl: true
  timeout: 0
}
