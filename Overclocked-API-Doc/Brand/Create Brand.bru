meta {
  name: Create Brand
  type: http
  seq: 3
}

post {
  url: {{host}}/brands
  body: json
  auth: inherit
}

body:json {
  {
    "Name": "Intel",
    "ImageUrl": "{{imageUrl}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
