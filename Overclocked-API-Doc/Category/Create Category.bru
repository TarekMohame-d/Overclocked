meta {
  name: Create Category
  type: http
  seq: 3
}

post {
  url: {{host}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "Name": "CPU",
    "ImageUrl": "{{imageUrl}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
